const path = require("path");

module.exports = {
  config: path.resolve("db", "config", "config.js"), // alteramos o arquivo para .js como monstra na documentação e adicionamos a pasta "db" no diretório.
  "models-path": path.resolve("db", "models"), // adicionamos a pasta "db" no diretório.
  "seeders-path": path.resolve("db", "seeders"),
  "migrations-path": path.resolve("db", "migrations"),
};
